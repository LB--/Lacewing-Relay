
On Windows, use the MSVC project file in the 'msvc' directory to build a DLL/LIB.

On Linux/BSD/OS X:

    ./autogen.sh
    ./configure
    make
    sudo make install
    
Node.JS module (after installing liblacewing):

    cd node
    node-waf configure build
    node-waf install
    
    
Lacewing is a framework for writing cross-platform, networked applications in C, C++ and Javascript.


INSTALLING
----------

On Windows, use the MSVC project file in the 'msvc' directory to build a DLL/LIB.

On Linux/BSD/OS X:

    ./configure
    make
    make install
    
Node.JS module:

    node-waf configure build
    node-waf install
    
    